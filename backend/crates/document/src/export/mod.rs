//! Dossier export serialisers — Markdown + JSON (compliance/01 §6.1, backend/01 §1.6.2). PDF is in
//! the sibling `pdf` module. Together with the PDF they form the三件套 (PDF + MD + JSON) bundle.

pub mod json;
pub mod md;
