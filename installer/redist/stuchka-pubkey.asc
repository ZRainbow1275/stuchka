SEAM-PLACEHOLDER: Stučka application upgrade-package GPG public key (master key).

This is NOT a real key. The real project keypair is generated once against the L0-01
legal subject and held offline; replace this file via:

    gpg --armor --export <upgrade-key-id> > keys/stuchka-pubkey.asc
    cp keys/stuchka-pubkey.asc installer/redist/

Until then, the upgrader's GPG-verify path (scripts/upgrade/apply-upgrade.ps1 Step 3 /
scripts/kb/verify_gpg.sh / crates/kb gpg::verify_detached) is exercised against a runtime
throwaway keypair in scripts/kb/selftest_gpg.sh, so the code path is real and tested while
the production key remains a documented seam.

Fingerprint (publish in README first screen once the real key exists): XXXX YYYY ZZZZ ...
